{
  "name": "auth-server",
  "version": "1.0.0",
  "description": "Better Auth server for Physical AI Book authentication",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "db:generate": "npx @better-auth/cli generate",
    "db:migrate": "npx @better-auth/cli migrate"
  },
  "dependencies": {
    "better-auth": "^1.0.0",
    "better-sqlite3": "^12.0.0",
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/express": "^5.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/cors": "^2.8.17",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
